@model ErrorViewModel

@{ 
    ViewData["Title"] = $"Erro: {(int)Model.StatusCode}";
}

<h3 class="text-danger">@ViewData["Title"]</h3>

@foreach (string error in Model.ErrorsMessage)
{
    <p class="text-danger">@error</p>
} 